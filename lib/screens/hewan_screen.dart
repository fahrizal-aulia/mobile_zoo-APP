import 'package:flutter/material.dart';

class HewanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Hewan')),
      body: Center(child: Text('Informasi Hewan')),
    );
  }
}
